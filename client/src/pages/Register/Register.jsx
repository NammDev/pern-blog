import classNames from 'classnames/bind'
import styles from './Register.module.scss'

const cx = classNames.bind(styles)

function Register() {
  return <div className={cx('register')}>Register</div>
}

export default Register
